# Shipping packet

Status: **showable** as a static, synthetic portfolio product.

## Verified 2026-07-16

- Product: tool-first first viewport, cross-domain problem framing, collapsed product thinking, truthful synthetic metrics, canonical approval logic, explicit simulated-AI label, and coherent empty state.
- Accessibility: skip link, landmarks, heading hierarchy, labelled controls, live result status, non-color selected cue, master-detail focus transfer, reduced motion, and clean 320px reflow.
- Security: strict CSP, no runtime network or untrusted HTML path, no secrets or PII, human-gated mock actions, pinned evaluator, and zero production dependency advisories.
- Performance: three local files, no runtime dependencies, fonts, images, or network calls. The full page is 19.8KB gzip (HTML + CSS + JavaScript), including 9.7KB gzip JavaScript. Target budgets are LCP <= 1.2s, INP <= 100ms, CLS <= 0.05, and <= 30KB gzip JavaScript on mobile 4G.
- Tests: 6/6 source-contract tests and JavaScript syntax pass; desktop/mobile search, filter, disclosure, focus, metrics, and console checks pass in-browser.

## Residual limits

All workflows and metrics are synthetic. Public technical documentation supports domain framing but does not substitute for user research. The page does not validate real transfer status, legal/regulatory policy, partner APIs, or live-model quality.

Supporting maps live in `docs/architecture.md`, `docs/flows.md`, `docs/permissions.md`, `docs/variables.md`, `docs/seo.md`, and `docs/tests.md`.
