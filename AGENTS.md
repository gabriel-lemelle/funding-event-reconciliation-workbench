# Agent guide

Read `SHIPPING.md` and the six files in `docs/` before changing behavior. Preserve the static/keyless boundary, synthetic-data disclosure, `auto_execute:false` approval posture, honest simulated-AI label, result live region, focus transfer, and 320px reflow.

Before merging, run `npm test`, `node --check app.js`, and `npm audit --omit=dev`; then execute the browser gate in `docs/tests.md`. Update `SHIPPING.md` when evidence or residual limits change.
